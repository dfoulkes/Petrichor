# Petrichor — project notes for Claude Code

ESP32 + ESPHome irrigation controller. Electrical/firmware side is built and
proven; see `specs/build-guide.md` and `docs/wiring-and-cable-grades.md`.

## Active hand-off

**Water-side rebuild is paused mid-task — see `docs/plumbing-rebuild.md`.**
One valve port cross-threaded; the valve needs replacing and the reducing
bushes are switching from brass to plastic. That note has the shopping list
(needs a browser to order) and the assembly method. Pick up there.
